FROM alpine:3.22
RUN apk add --no-cache ca-certificates tzdata && addgroup -S app && adduser -S -G app app
COPY .deploy/library-sync /usr/local/bin/library-sync
USER app
ENTRYPOINT ["/usr/local/bin/library-sync"]
